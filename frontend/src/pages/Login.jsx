import { useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import Brand from '../components/Brand'
import { api, saveAuth } from '../api'

export default function Login(){
  const navigate=useNavigate(); const [form,setForm]=useState({email:'',password:'',role:'farmer'}); const [error,setError]=useState(''); const [busy,setBusy]=useState(false)
  const submit=async e=>{e.preventDefault();setError('');setBusy(true);try{const data=await api('/api/auth/login',{method:'POST',body:JSON.stringify(form)});saveAuth(data);navigate(data.user.role==='admin'?'/admin':'/dashboard')}catch(e){setError(e.message)}finally{setBusy(false)}}
  return <div className="auth-page"><div className="auth-art"><Brand/><div className="auth-art-copy"><span className="pill light">YieldSense AI</span><h1>One sign-in for your agricultural workspace.</h1><p>Farmers can manage farms, forecasts, soil records, weather checks and recommendations. Administrators manage platform data and access.</p></div><div className="auth-quote">Field information becomes useful when it stays organized.</div></div><div className="auth-panel"><Link to="/" className="back-link">← Back to home</Link><form className="auth-card" onSubmit={submit}><div><span className="eyebrow">Secure access</span><h2>Sign in</h2><p>Select the role linked to your account.</p></div><div className="role-toggle"><button type="button" className={form.role==='farmer'?'active':''} onClick={()=>setForm({...form,role:'farmer'})}>🌱 Farmer</button><button type="button" className={form.role==='admin'?'active':''} onClick={()=>setForm({...form,role:'admin'})}>🛡️ Admin</button></div><label>Email address<input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@example.com"/></label><label>Password<input type="password" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Minimum 8 characters"/></label>{error&&<div className="error-box">{error}</div>}<button className="btn primary wide" disabled={busy}>{busy?'Signing in…':'Sign in →'}</button>{form.role==='farmer'?<p className="auth-foot">New farmer? <Link to="/register">Create your account</Link></p>:<p className="auth-note">Admin accounts are created with <code>python scripts/create_admin.py</code> in the backend.</p>}</form></div></div>
}
